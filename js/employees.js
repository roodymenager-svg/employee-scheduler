window.editEmployeeEmail = function(index){const name=window.contactNames[index],email=prompt(`Email address for ${name}:`,contacts[name]||'');if(email===null)return;if(email.trim()&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.trim()))return show('Enter a valid email address, or leave it blank.',true);contacts[name]=email.trim();render();queueAutoSave();show(`Email address updated for ${name}.`)};
window.addEmployee = function(){const name=prompt('Employee full name:');if(!name||!name.trim())return;const email=prompt('Employee email address (optional):','');if(email&& !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.trim()))return show('Enter a valid email address, or leave it blank.',true);contacts[name.trim()]=email.trim();render();queueAutoSave();show(`${name.trim()} was added to the employee list.`)};
